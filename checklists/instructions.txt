---THE ONLY
--CHECKLIST USAGE
?.- The checklist will automatically monitor your actions 
?.use the standard scroll keys and select key for checklist 
?.manipulation or use the scroll and V button on the
?.pedestal
?.- The check items marked by the gray square on the
?.left require checking - select key on the item to check 
?.it
?.- Any item can be overriden by selecting it 
?.- The whole list can be overriden by selecting COMPLT a
?.list can be reset by selecting RESET